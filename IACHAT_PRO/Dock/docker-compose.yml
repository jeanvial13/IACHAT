version: "3.9"

services:
  iachatpro:
    build:
      context: ..
      dockerfile: Dock/Dockerfile
    container_name: iachatpro
    ports:
      - "8080:8080"
    environment:
      - OPENAI_API_KEY=${OPENAI_API_KEY}
      - OPENAI_MODEL=${OPENAI_MODEL:-gpt-4o-mini}
    restart: unless-stopped
